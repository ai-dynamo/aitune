# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

[project]
name = "ai-tune-flux-example"
version = "0.1.0"
description = "NVIDIA AITune FLUX.1 Example"
readme = "README.md"
license = { text = "Apache 2.0" }
requires-python = ">=3.10, <3.13"

dependencies = [
    "aitune",
    "aitune-examples-common",
    "diffusers>=0.25.0,<0.35",
    "torch>=2.0.0",
    "transformers<5",
    "pillow",
    "sentencepiece",
    "protobuf",
]

[project.optional-dependencies]
dynamo = [
    "ai-dynamo>=1.3,<2.0.0",
    "openai",
    "uvicorn",
    "aiohttp",
    "aiofiles",
    "uvloop",
    "pyyaml",
]

[tool.uv.sources]
aitune = { path = "../../", editable = true }
aitune-examples-common = { path = "../../examples/common" }

[project.scripts]
tune = "flux.tune:main"
inference = "flux.inference:main"

[build-system]
requires = ["setuptools>=65.3.0", "wheel>=0.37.1"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
namespaces = false
include = ["flux"]

[tool.setuptools.package-data]
flux = ["**/*"]

[tool.aitune]
additional_tags = ["gpu/sm/120"]
use_gated_hf_token = true
allow_failure = true
